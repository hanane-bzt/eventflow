
services:

 mongo:
  image: mongo:7
  ports:
   - "27017:27017"

 backend:
  build: ./server
  ports:
   - "4000:4000"
  depends_on:
   - mongo

 frontend:
  build: ./client
  ports:
   - "5173:5173"
  depends_on:
   - backend
